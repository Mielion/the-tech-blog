<h2>Your Dashboard</h2>


{{#each posts as |post|}}
    <div class="post">
        <h3>{{post.title}}</h3>
        <p>Posted on {{post.date_created}}</p>
        <p>{{post.description}}</p>
        
    </div>
{{/each}}